export default function LabelCard( {textLabel}: {textLabel: string} ){
      return(
            <p className=" w-max py-1 px-3 rounded-full border border-gold bg-cream uppercase text-[10px] tracking-widest text-chocolate ">{textLabel}</p>
      )
}

/* Quitar el borde unavex puesta en la imagen
 */